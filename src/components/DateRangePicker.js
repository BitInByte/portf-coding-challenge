import styled from 'styled-components';

const DateRangePickerEl = styled.div``;

const DateRangePicker = () => {
  return (
    <DateRangePickerEl data-testid="date-range-picker"></DateRangePickerEl>
  );
};

export default DateRangePicker;
